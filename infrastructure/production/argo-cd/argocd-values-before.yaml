  ingress:
    enabled: false 
    labels: {}

    annotations:
      kubernetes.io/ingress.class: nginx
      nginx.ingress.kubernetes.io/ssl-redirect: "false"
      # cert-manager.io/cluster-issuer: letsencrypt-prod
      cert-manager.io/cluster-issuer: letsencrypt-staging


    ## Argo Ingress.
    ## Hostnames must be provided if Ingress is enabled.
    ## Secrets must be manually created in the namespace
    ##
    hosts:
      - raelix-cluster.duckdns.org

    paths:
      - /argocd
    tls:
      - secretName: argocd-server-tls
        hosts:
          - raelix-cluster.duckdns.org

server:
  config:
    url: "https://raelix-cluster.duckdns.org:8443/argocd"
    # Argo CD instance label key
    application.instanceLabelKey: argocd.argoproj.io/instance

    # DEPRECATED: Please instead use configs.credentialTemplates and configs.repositories
    # repositories: |
    #   - url: git@github.com:group/repo.git
    #     sshPrivateKeySecret:
    #       name: secret-name
    #       key: sshPrivateKey
    #   - type: helm
    #     url: https://charts.helm.sh/stable
    #     name: stable
    #   - type: helm
    #     url: https://argoproj.github.io/argo-helm
    #     name: argo

    oidc.config: |
      name: Keycloak
      issuer: https://raelix-cluster.duckdns.org:8443/auth/realms/master
      clientID: argocd
      clientSecret: argocd
      requestedIDTokenClaims:
        groups:
          essential: true
      requestedScopes:
        - openid
        - profile
        - email

# redis-ha:
#   enabled: true

# controller:
#   enableStatefulSet: true

# server:
#   autoscaling:
#     enabled: true
#     minReplicas: 2

# repoServer:
#   autoscaling:
#     enabled: true
#     minReplicas: 2